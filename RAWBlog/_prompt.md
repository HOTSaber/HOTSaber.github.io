# 显示可读且可复制的公式
```
- 名称：这是“Markdown + LaTeX 数学公式”的格式（行内用 ......，块级用 \[ ... \]）。Word 也能直接粘贴并识别这些公式。

- 如果想要 Word 的线性公式格式，请求“UnicodeMath 线性公式”（如 TF_c(a)=...），不含 TeX 命令。

你可以这样写 prompt

- 要求 Markdown+LaTeX

- “请用中文，按小标题分段，用 Markdown 展示；数学用 LaTeX：行内用 ......，块级用 \[ ... \]；每个公式单独成块，可直接复制到 Word；不要图片或截图。”

- 要求 UnicodeMath（Word 线性）

- “请输出可直接粘贴到 Word 方程编辑器的线性（UnicodeMath）公式，不用 $ 或代码块语言标记，每行一条公式，并用短中文说明。”
```